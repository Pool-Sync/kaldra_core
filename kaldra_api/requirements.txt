fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.8.2
numpy==2.1.0

# Dependência local do kaldra_core (instalado via path no docker / env)
# kaldra_core será montado no mesmo container/projeto
requests
