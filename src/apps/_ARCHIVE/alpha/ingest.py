"""
KALDRA-ALPHA — Earnings call ingestion (placeholder).
"""
from __future__ import annotations


def ingest_source(raw_text: str) -> str:
    """
    Placeholder ingestion: currently returns the text as-is.

    Args:
        raw_text: Raw earnings call transcript text

    Returns:
        Processed text (currently unmodified)
    """
    return raw_text
